import { FiltreEntrenadorPipe } from './filtre-entrenador-pipe';

describe('FiltreEntrenadorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreEntrenadorPipe();
    expect(pipe).toBeTruthy();
  });
});
